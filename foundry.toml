[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
test = "test"
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
broadcast = "broadcast-local"
fs_permissions = [{ access = "read-write", path = "./deployments" }]
